using System;
namespace Typeconversion{
	class typeconversion{
		static void Main(string[] args)
		{
			double d = 1.5;
			int i;
			
			i = (int)d;
			Console.WriteLine(i);
			Console.ReadKey();
			/* To run this program in command prompt 
			   follw this instructions
			   1.open command prompt as developer command prompt;
			   2. goto to the program directory.
			   3. type "csc programname.cs"
			   4. again type the program name only wthout extension.
			*/
		}
	}
}